spring:
  application:
     name: home-resource-ms
  cloud:
    config:
      failFast: true
      discovery:
        enabled: true
eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8100